# Rsync exclude patterns for sync-campusclaw.sh.
# Paths are interpreted relative to the rsync source root (build/campusclaw/src/.../).
# Lines starting with '#' are comments; blank lines ignored.
#
# Files listed here exist only on the campusclaw side (added directly,
# not derived from modules/*). The sync script must NOT overwrite or delete them.
# When you intentionally add a new file under campusclaw/ that has no
# counterpart in modules/*, add its path here so future syncs preserve it.
com/huawei/hicampus/claw/skills/
com/huawei/hicampus/claw/codingagent/config/CampusMateConfigurationTest.java
