# Device registry (canonical)

**Do not edit `registry.json` here.**

Campus-device-ops reads the canonical device registry from **device-inspection-re**:

`../device-inspection-re/mock_fixtures/devices/registry.json`

Ops-only fields (`assigneeId`, `status`) live in **`assignee_map.json`** in this directory.
