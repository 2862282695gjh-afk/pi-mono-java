-- CampusClaw Session storage has no static business seed data.
-- Each Session transaction creates its own rows in all required tables.
-- Execute after 01_session_schema.sql during a full installation only.

BEGIN;

-- Intentionally empty.

COMMIT;
